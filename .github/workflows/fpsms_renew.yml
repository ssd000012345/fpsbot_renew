name: fps.ms 每日续期

on:
  schedule:
    # 每天 UTC 02:00 执行（北京时间 10:00），可按需修改
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      debug_mode:
        description: '调试模式（打印更多日志）'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  renew:
    name: 执行续期
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
      actions: write

    steps:
      # ── 1. 拉取仓库 ─────────────────────────────────────────────
      - name: 拉取仓库
        uses: actions/checkout@v4

      # ── 2. 安装 Python ──────────────────────────────────────────
      - name: 安装 Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: 'requirements.txt'

      # ── 3. 安装 Python 依赖 ──────────────────────────────────────
      - name: 安装 Python 依赖
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      # ── 4. 安装系统依赖（Chrome 运行时 + 录屏工具） ──────────────
      - name: 安装系统依赖
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            libnss3 libxss1 libasound2t64 libatk-bridge2.0-0 \
            libgtk-3-0 libgbm1 libxkbcommon0 \
            fonts-wqy-zenhei fonts-noto-cjk \
            jq bc curl \
            xvfb x11-utils \
            ffmpeg

      # ── 5. 检查出口 IP ────────────────────────────────────────────
      - name: 检查出口 IP
        run: |
          echo "[INFO] 当前出口 IP:"
          curl -s --max-time 10 https://api.ipify.org && echo

      # ── 6. 运行续期脚本（含录屏） ─────────────────────────────────
      - name: 运行 fps.ms 续期脚本
        id: run_renew
        env:
          FPSMS_DISCORD_TOKEN: ${{ secrets.FPSMS_DISCORD_TOKEN }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          DISPLAY: ':99'
        run: |
          mkdir -p screenshots recordings

          # 启动虚拟显示
          Xvfb :99 -screen 0 1280x800x24 &
          XVFB_PID=$!
          sleep 2
          echo "[INFO] Xvfb 已启动 (PID=$XVFB_PID)"

          # 启动 ffmpeg 录屏
          ffmpeg -y \
            -f x11grab \
            -r 10 \
            -video_size 1280x800 \
            -i :99 \
            -vcodec libx264 \
            -preset ultrafast \
            -crf 30 \
            recordings/session.mp4 \
            2>recordings/ffmpeg.log &
          FFMPEG_PID=$!
          echo "[INFO] ffmpeg 录屏已启动 (PID=$FFMPEG_PID)"
          sleep 1

          echo "=============================="
          echo " 开始执行 fps.ms 续期脚本"
          echo " 时间: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "=============================="

          python renew.py 2>&1 | tee renew.log
          EXIT_CODE=${PIPESTATUS[0]}

          echo ""
          echo "=============================="
          echo " 脚本退出码: $EXIT_CODE"
          echo "=============================="

          # 优雅停止 ffmpeg
          echo "[INFO] 停止录屏..."
          kill -INT $FFMPEG_PID 2>/dev/null || true
          wait $FFMPEG_PID 2>/dev/null || true

          # 停止 Xvfb
          kill $XVFB_PID 2>/dev/null || true

          # 检查录像文件
          if [ -f recordings/session.mp4 ]; then
            SIZE=$(du -sh recordings/session.mp4 | cut -f1)
            echo "[INFO] 录像文件: recordings/session.mp4 ($SIZE)"
          else
            echo "[WARN] 录像文件未生成，检查 recordings/ffmpeg.log"
            cat recordings/ffmpeg.log || true
          fi

          exit $EXIT_CODE

      # ── 7. 上传截图 ───────────────────────────────────────────────
      - name: 上传调试截图
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: renew-screenshots-${{ github.run_number }}
          path: screenshots/
          retention-days: 3
          if-no-files-found: ignore

      # ── 8. 上传录屏视频 ────────────────────────────────────────────
      - name: 上传录屏视频
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: renew-recording-${{ github.run_number }}
          path: recordings/
          retention-days: 3
          if-no-files-found: ignore

      # ── 9. 清理旧的 workflow 运行记录 ────────────────────────────
      - name: 清理历史运行记录
        if: always()
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          delete_workflow_pattern: ${{ github.workflow }}
          retain_days: 0
          keep_minimum_runs: 2
